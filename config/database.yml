default: &default
  adapter: postgresql
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  database: <%= ENV["DEV"] %>

test:
  <<: *default
  database:  <%= ENV["TESTDB"] %>

production:
  url: <%= ENV["DATABASE_URL"] %>
